require_relative 'spec_helper'


feature "User browing the website" do 
	context "on Argument page" do

		before(:all) do 
			@user = User.create(username: "test", email: "test@test.com", password: "test")
			@conclusion = Claim.create(author_id: @user.id, body: "Fuck this shitty claim or conc or w/e")
			@argument = Argument.create(author_id: @user.id, conclusion_id: @conclusion.id, is_supporting: true)
		end

		it "sees an argument with an author" do
			visit("/arguments/#{@argument.id}")
			find_link('Fuck this shitty claim or conc or w/e').visible?
		end

	end
end