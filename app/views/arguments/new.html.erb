<%#= arg.error_messages %>

<%= form_for @argument do |arg| %>

	<!-- Arguments Conclusion -->
	<%= fields_for :conclusion, @argument.conclusion do |conc| %>
	  <%= conc.label :body, 'Conclusion:' %>
	  <%= conc.text_field :body %>
	<br><br>

		<!-- Supporting/Opposing Radio Tags -->
		<%= conc.label(:is_supporting, "Supporting:") %>
		<%= conc.radio_button(:is_supporting, true) %>
		<%= conc.label(:is_supporting, "Opposing:") %>
		<%= conc.radio_button(:is_supporting, false) %>
	<br><br>

	<% end -%>
	<!-- Arguments Premises -->
	<% @argument.premises.each_with_index do |prem, i|%>
	  <%= fields_for "premise#{i+1}" do |p| %>
	    <%= p.label :body, "Premise #{i+1}" %>
	    <%= p.text_field :body %>
	  	<%= p.label :_destroy, "Delete" %>
	  	<%= p.check_box :_destroy%>
	    <% end -%>
	    <br>
	<% end -%>

	<!-- Submit tag -->
  <%= submit_tag("submit") %>

<% end %>